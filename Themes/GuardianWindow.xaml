<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ArkPilot.Controls"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    

    <Style TargetType="{x:Type controls:GuardianWindow}">

        <Setter Property="WindowStyle"
            Value="None"/>

        <Setter Property="AllowsTransparency"
            Value="False"/>

        <Setter Property="Background"
            Value="{DynamicResource GuardianBrush.Background}"/>

        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome
            CaptionHeight="32"
            ResizeBorderThickness="6"
            CornerRadius="0"
            GlassFrameThickness="0"/>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="{x:Type controls:GuardianWindow}">

                    <Border
                Background="{TemplateBinding Background}"
                BorderBrush="{DynamicResource GuardianBrush.Border}"
                BorderThickness="1">

                        <AdornerDecorator>

                            <Grid>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid
    Grid.Row="0"
    Background="{DynamicResource GuardianBrush.Surface}">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="42"/>
                                        <ColumnDefinition Width="42"/>
                                        <ColumnDefinition Width="42"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock
        Grid.Column="0"
        Margin="12,0"
        VerticalAlignment="Center"
        Text="{TemplateBinding Title}"
        Foreground="{DynamicResource GuardianBrush.Text}"
        FontWeight="SemiBold"/>

                                    <Button
        Grid.Column="1"
        Content="─"
        FontSize="14"
        Background="Transparent"
        Foreground="{DynamicResource GuardianBrush.Text}"
        BorderThickness="0"
        Command="{x:Static SystemCommands.MinimizeWindowCommand}"
        CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"                                
        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>

                                    <Button
        x:Name="MaximizeButton"                                
        Grid.Column="2"
        Content="□"
        FontSize="13"
        Background="Transparent"
        Foreground="{DynamicResource GuardianBrush.Text}"
        BorderThickness="0"
        Command="{x:Static SystemCommands.MaximizeWindowCommand}"
        CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"                               
        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>

                                    <Button
        Grid.Column="3"
        Content="✕"
        FontSize="13"
        Background="Transparent"
        Foreground="{DynamicResource GuardianBrush.Text}"
        BorderThickness="0"
        Command="{x:Static SystemCommands.CloseWindowCommand}"
        CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"                                
        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>

                                </Grid>
                                
                                <ContentPresenter
        Grid.Row="1"
        Content="{TemplateBinding Content}"
        ContentTemplate="{TemplateBinding ContentTemplate}"
        ContentStringFormat="{TemplateBinding ContentStringFormat}"/>

                            </Grid>
                            
                        </AdornerDecorator>

                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="WindowState"
             Value="Maximized">

                            <Setter
            TargetName="MaximizeButton"
            Property="Command"
            Value="{x:Static SystemCommands.RestoreWindowCommand}"/>

                            <Setter
            TargetName="MaximizeButton"
            Property="Content"
            Value="❐"/>

                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>
        </Setter>


    </Style>
    
</ResourceDictionary>